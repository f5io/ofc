{
  "name": "graphql-federation",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "develop": "ofc build && now dev -A now.ofc.json",
    "prod": "ofc build prod && now -A now.ofc.json",
    "watch": "ofc watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": {
    "nohoist": [ "**" ]
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "12.x"
  },
  "dependencies": {
    "koa": "^2.11.0"
  },
  "devDependencies": {
    "@apollo/federation": "^0.11.2",
    "@apollo/gateway": "^0.11.4",
    "apollo-server-koa": "^2.9.13",
    "bufferutil": "^4.0.1",
    "@ofc/cli": "^0.0.1-alpha.4",
    "utf-8-validate": "^5.0.2"
  }
}
